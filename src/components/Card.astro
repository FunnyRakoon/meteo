---
const { first, last, genre, email, phone, picture } = Astro.props;
---

<article
  class="bg-white shadow-md rounded-lg p-6 transition-transform duration-300 hover:scale-105 hover:shadow-xl text-center max-w-sm mx-auto"
>
  <div class="flex justify-center mb-4">
    <img
      src={picture}
      alt={`${first} ${last}`}
      class="w-32 h-32 rounded-full object-cover border-4 border-blue-500 transition duration-300 hover:border-pink-500"
    />
  </div>

  <h2
    class="text-xl font-bold mb-2 text-gray-800 transition-all duration-500 hover:tracking-wide hover:text-blue-600"
  >
    {first} {last}
  </h2>

  <p class="text-sm text-gray-600 italic mb-1 capitalize">Genre: {genre}</p>
  <p class="text-sm text-gray-700 mb-1">{email}</p>
  <p class="text-sm text-gray-700">{phone}</p>
</article>
